package constant

// Note, here is just a set of duplication error codes from the
// 'microservice-module-error (https://bitbucket.org/v-cube/microservice-module-error/src)' repo,
// which is built in JavaScript, and also is the control center.
const (
	SnapshotDeltaGenIP    string = "SE1"
	InvalidDeviceBucketId string = "SE2"
)
